export const labReadmes: Record<string, string> = {
  "crypto-basic": `# 密码学基础 · [基础] 对称/公钥/哈希/签名：正确使用与误用识别

## 实验目标

通过亲手实现并验证四类密码学原语，理解"教材抽象 → 程序实现"的映射，并能识别常见误用：

1. **对称加密**：AES-CBC / AES-GCM 加解密，理解 IV、填充、认证标签的作用。
2. **公钥加密**：RSA-OAEP 加解密，理解密钥长度与填充模式。
3. **哈希**：SHA-256 摘要计算，理解抗碰撞性与"加盐"。
4. **数字签名**：RSA-PSS / Ed25519 签名与验签，理解签名与加密的区别。

## 实验要求

1. 用 Python（cryptography 库）或你熟悉的语言，逐个实现上述四类原语。
2. 重点：先写出"算法步骤"，再映射到代码中的变量与数据结构，最后用测试向量验证。
3. 故意构造一处误用（如 ECB 模式），观察并解释其安全弱点。
4. 在报告中识别并说明**至少两处误用**（ECB、复用 IV、无认证加密、MD5 签名、固定盐值等）。

## 验收标准

- **可复现实物**：实现代码文件（如 solution.py），可在本机重复运行。
- **验证证据**：程序运行输出正确（加解密/签名验签结果一致）。
- **理解性说明**：
  - 每类原语的"算法 → 数据结构 → 代码"映射；
  - 至少两处误用的识别、危害与修复。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "crypto-inter": `# 密码学基础 · [进阶] PKI 证书链与 TLS 握手抓包分析

## 实验目标

理解 PKI 信任模型与 TLS 握手过程，能通过抓包定位关键字段并解释证书验证机制。

## 实验要求

1. 使用 OpenSSL 签发一条完整的证书链（Root CA → Intermediate CA → End Entity）。
2. 用 curl/openssl s_client 连接一个 HTTPS 站点，验证证书链完整性。
3. 用 Wireshark/tcpdump 抓取 TLS 握手包，标注 ClientHello、ServerHello、Certificate、Finished 等关键消息。
4. 解释：证书中哪些字段参与验证？浏览器如何判断证书可信？

## 验收标准

- **可复现实物**：证书链文件（root.pem / intermediate.pem / server.pem）+ 签发脚本。
- **验证证据**：抓包截图/pcap 文件 + openssl verify 输出。
- **理解性说明**：TLS 握手每一步的作用；证书链验证的信任传递逻辑。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "crypto-adv": `# 密码学基础 · [挑战] MITM 构造与信任链失效分析

## 实验目标

在给定威胁模型下构造中间人（MITM）条件，分析 PKI 信任链失效原因并给出可验证的修复策略。

## 实验要求

1. 构造一个本地 MITM 场景：使用 mitmproxy 或自签证书拦截 HTTPS 流量。
2. 分析信任链在哪个环节被绕过（客户端未校验证书？接受了自签 CA？）。
3. 给出修复方案（证书固定 / HSTS / CT 日志检查等）并验证修复有效。
4. 明确说明威胁模型：攻击者能力、攻击前提条件、防护边界。

## 验收标准

- **可复现实物**：MITM 环境搭建脚本 + 修复配置。
- **验证证据**：攻击成功的抓包证据 + 修复后攻击失败的证据。
- **理解性说明**：威胁模型描述；信任链失效点分析；修复方案的安全论证。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "web-sec-basic": `# Web 安全 · [基础] SQLi/XSS/CSRF 复现与最小修复

## 实验目标

复现三种经典 Web 漏洞（SQL 注入、XSS、CSRF），理解其原理并实施最小化修复。

## 实验要求

1. 搭建一个含漏洞的简单 Web 应用（可用 Flask/Express/PHP），包含登录、留言等功能。
2. 分别复现：SQL 注入（绕过登录或读取数据）、反射型 XSS、CSRF（跨站修改密码）。
3. 对每种漏洞实施最小修复（参数化查询、输出编码、CSRF Token）。
4. 验证修复后攻击不再生效。

## 验收标准

- **可复现实物**：漏洞应用代码 + 攻击 payload + 修复后代码。
- **验证证据**：攻击成功截图/日志 + 修复后攻击失败证据。
- **理解性说明**：每种漏洞的根因、攻击向量、修复原理。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "web-sec-inter": `# Web 安全 · [进阶] 容器化部署 + 日志审计与防护配置

## 实验目标

将 Web 应用容器化部署，配置日志审计与安全防护策略（CSP、WAF 规则等），用证据支撑"修复有效"。

## 实验要求

1. 将基础层的漏洞应用用 Docker 容器化部署。
2. 配置 Nginx 反向代理 + CSP 头 + 访问日志。
3. 配置应用层日志（记录所有输入参数、异常请求）。
4. 重放基础层的攻击 payload，从日志中定位攻击痕迹并验证 CSP 阻断了 XSS。

## 验收标准

- **可复现实物**：docker-compose.yml + Nginx 配置 + 应用代码。
- **验证证据**：CSP 阻断 XSS 的浏览器控制台截图 + 日志中的攻击记录。
- **理解性说明**：CSP 各指令的作用；日志审计如何辅助攻击溯源。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "web-sec-adv": `# Web 安全 · [挑战] 多框架/多配置变体：对比防护效果与代价

## 实验目标

在多种框架/配置变体下对比同一漏洞的防护效果与性能代价。

## 实验要求

1. 选择同一个漏洞场景（如 XSS），在至少两种框架/配置下实现防护（如：CSP strict-dynamic vs nonce-based；React 自动转义 vs 手动 DOMPurify）。
2. 对比防护效果：哪些攻击向量被阻断？哪些仍可绕过？
3. 对比代价：开发复杂度、性能影响、兼容性问题。
4. 给出"在什么场景下选哪种方案"的结论。

## 验收标准

- **可复现实物**：至少两套防护实现代码 + 测试用例。
- **验证证据**：对比测试结果表格（攻击向量 × 防护方案 → 通过/阻断）。
- **理解性说明**：各方案优劣分析与场景推荐。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "os-sec-basic": `# 操作系统安全 · [基础] 权限边界与 Set-UID：安全配置与风险点

## 实验目标

理解 Linux 权限模型与 Set-UID 机制，识别不当配置带来的提权风险。

## 实验要求

1. 编写一个 Set-UID 程序（C 语言），观察其以 root 权限执行的行为。
2. 演示 Set-UID 程序的典型安全风险（如：通过 system() 调用 shell 命令）。
3. 修复：将 system() 替换为 execve()，对比修复前后的安全差异。
4. 列出系统中现有的 Set-UID 程序，评估哪些存在风险。

## 验收标准

- **可复现实物**：Set-UID 程序源码（漏洞版 + 修复版）+ 编译脚本。
- **验证证据**：漏洞利用成功获取 root shell 的截图 + 修复后失败的证据。
- **理解性说明**：Set-UID 机制原理；system() vs execve() 的安全差异。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "os-sec-inter": `# 操作系统安全 · [进阶] 环境变量/命令注入：复现与防护

## 实验目标

理解环境变量注入（PATH、LD_PRELOAD）与命令注入的攻击面，实施防护。

## 实验要求

1. 通过修改 PATH 环境变量，劫持 Set-UID 程序中 system() 调用的命令。
2. 通过 LD_PRELOAD 注入自定义共享库，劫持程序中的库函数调用。
3. 演示命令注入：构造特殊输入使程序执行非预期命令。
4. 防护：清理环境变量、使用绝对路径、输入校验。

## 验收标准

- **可复现实物**：攻击脚本 + 恶意 so/命令 + 防护后代码。
- **验证证据**：攻击成功日志 + 防护后攻击失败证据。
- **理解性说明**：PATH/LD_PRELOAD 攻击原理；防护措施的完整性分析。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "os-sec-adv": `# 操作系统安全 · [挑战] TOCTOU 竞态条件：机制理解 + 证据链

## 实验目标

理解 TOCTOU（Time-of-Check to Time-of-Use）竞态条件，构造竞争窗口并提供证据链。

## 实验要求

1. 编写一个存在 TOCTOU 漏洞的 Set-UID 程序（先 access() 检查权限，再 open() 打开文件）。
2. 编写攻击程序：在 access 和 open 之间快速切换符号链接，使程序打开 /etc/shadow 等敏感文件。
3. 多次运行直到竞争成功，记录成功率。
4. 防护：使用 open() + fstat() 替代 access() + open()。

## 验收标准

- **可复现实物**：漏洞程序 + 攻击程序 + 防护版本。
- **验证证据**：攻击成功读取敏感文件的输出 + 竞争成功率统计。
- **理解性说明**：TOCTOU 的时间窗口机制；为什么 open+fstat 能防护。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "db-sec-basic": `# 数据库安全 · [基础] 注入风险与最小权限原则

## 实验目标

理解 SQL 注入原理，实践数据库最小权限配置。

## 实验要求

1. 搭建一个含 SQL 注入漏洞的应用（拼接 SQL 语句的登录/查询接口）。
2. 演示注入攻击：绕过登录、读取其他用户数据、UNION 注入。
3. 修复：改用参数化查询（Prepared Statement）。
4. 配置数据库用户权限：应用账号只有 SELECT/INSERT，无 DROP/ALTER。

## 验收标准

- **可复现实物**：漏洞应用 + 修复应用 + 数据库权限配置脚本。
- **验证证据**：注入成功输出 + 修复后注入失败 + SHOW GRANTS 输出。
- **理解性说明**：注入根因（字符串拼接 vs 参数化）；最小权限的意义。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "db-sec-inter": `# 数据库安全 · [进阶] 账号权限/备份/日志泄露/凭据管理

## 实验目标

审计数据库安全配置，识别权限过大、备份泄露、日志暴露凭据等常见问题。

## 实验要求

1. 审计一个数据库实例：检查是否存在多个应用共用 root 账号的情况。
2. 检查备份文件权限：.sql 备份是否对外可读？是否包含明文密码？
3. 检查应用日志：是否记录了 SQL 语句中的用户密码/敏感数据？
4. 实施凭据管理：使用环境变量或密钥管理服务存储数据库密码，不硬编码。

## 验收标准

- **可复现实物**：审计脚本 + 修复配置 + 凭据管理方案。
- **验证证据**：审计发现清单 + 修复前后对比。
- **理解性说明**：各风险点的攻击场景与影响分析。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "db-sec-adv": `# 数据库安全 · [挑战] 功能需求→安全约束：配置方案设计与验证

## 实验目标

在"功能需求—安全约束"双重条件下设计数据库安全配置方案，并以测试与日志证据验证。

## 实验要求

1. 给定场景：一个多租户 SaaS 应用，需要行级安全（RLS）+ 审计日志 + 自动备份。
2. 设计方案：用户隔离（schema/RLS）、审计策略（哪些操作记日志）、备份策略（频率/加密）。
3. 实现并验证：租户 A 无法访问租户 B 数据；审计日志记录了关键操作；备份可恢复。
4. 分析代价：RLS 对查询性能的影响、审计日志的存储成本。

## 验收标准

- **可复现实物**：数据库 schema + RLS 策略 + 审计配置 + 备份脚本。
- **验证证据**：隔离测试结果 + 审计日志样本 + 备份恢复验证。
- **理解性说明**：方案设计决策依据；安全与性能的权衡分析。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "sw-sec-basic": `# 软件安全 · [基础] 缓冲区溢出/格式化字符串：漏洞机理

## 实验目标

理解栈缓冲区溢出与格式化字符串漏洞的触发条件和利用原理。

## 实验要求

1. 编写一个含栈溢出漏洞的 C 程序（gets/strcpy 无边界检查）。
2. 构造输入覆盖返回地址，使程序跳转到指定函数（如 getShell）。
3. 编写一个含格式化字符串漏洞的程序（printf(user_input)）。
4. 利用 %x/%n 读取/写入内存，解释其原理。
5. 编译时关闭保护：-fno-stack-protector -z execstack -no-pie。

## 验收标准

- **可复现实物**：漏洞程序源码 + exploit 输入/脚本 + Makefile。
- **验证证据**：成功劫持控制流的截图/输出（如弹出 shell 或打印特定字符串）。
- **理解性说明**：栈帧布局图；EIP 覆盖原理；格式化字符串的 %n 写入机制。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "sw-sec-inter": `# 软件安全 · [进阶] ASLR/Canary/NX 防护机制对比验证

## 实验目标

理解三种主要防护机制（ASLR、Stack Canary、NX）的原理，对比验证"为何能/不能利用"。

## 实验要求

1. 分别在开启/关闭各保护的条件下尝试基础层的 exploit：
   - 只开 NX：栈不可执行，shellcode 失败。
   - 只开 Canary：覆盖返回地址前触发 __stack_chk_fail。
   - 只开 ASLR：地址随机化导致硬编码地址失效。
2. 记录每种情况下的程序行为（崩溃信息、段错误、检测到栈破坏等）。
3. 解释每种机制的防护原理与局限性。

## 验收标准

- **可复现实物**：Makefile（不同编译选项）+ 测试脚本。
- **验证证据**：6 种组合的运行结果对比表。
- **理解性说明**：每种机制如何阻断攻击；各自的绕过思路（概念层面）。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "sw-sec-adv": `# 软件安全 · [挑战] 给定二进制与约束：最小复现 + 关键步骤解释

## 实验目标

在所有保护开启的条件下，对给定二进制完成最小可复现利用并解释关键步骤。

## 实验要求

1. 给定一个编译好的二进制（开启 NX + Canary，部分 ASLR），找到漏洞点。
2. 构造 exploit：信息泄露（leak canary/libc base）→ ROP chain → getshell。
3. 每一步给出解释：为什么需要 leak？ROP gadget 如何选取？
4. 记录完整利用过程（可用 pwntools 脚本）。

## 验收标准

- **可复现实物**：exploit 脚本（如 exploit.py）+ 目标二进制。
- **验证证据**：成功 getshell 的截图/日志。
- **理解性说明**：漏洞定位过程；每步 exploit 的原因与作用；如果失败说明卡在哪。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "net-sec-basic": `# 网络安全 · [基础] 嗅探与伪造：DNS/TCP 典型攻击

## 实验目标

使用抓包工具嗅探网络流量，复现 ARP 欺骗与 DNS 伪造攻击。

## 实验要求

1. 使用 Wireshark/tcpdump 抓包，识别 HTTP 明文传输中的敏感信息。
2. 使用 arpspoof/scapy 实施 ARP 欺骗，将目标流量引导至攻击机。
3. 使用 scapy/dnschef 实施 DNS 伪造，将目标域名解析到恶意 IP。
4. 解释每种攻击的网络协议层原理。

## 验收标准

- **可复现实物**：攻击脚本 + 网络拓扑说明（可用 Docker 容器模拟多机）。
- **验证证据**：抓包文件（pcap）显示欺骗成功 + 目标访问被劫持。
- **理解性说明**：ARP 协议无认证的弱点；DNS 响应抢答机制。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "net-sec-inter": `# 网络安全 · [进阶] 防火墙策略探索 + VPN 机制与验证

## 实验目标

理解防火墙规则编写与 VPN 隧道机制，能验证策略有效性。

## 实验要求

1. 使用 iptables/nftables 编写防火墙规则：允许 SSH + HTTP，拒绝其他入站。
2. 验证规则有效性：用 nmap/curl 从外部测试端口开放情况。
3. 搭建 WireGuard/IPSec VPN 隧道（两个容器之间）。
4. 验证隧道加密：抓包显示隧道内流量已加密，明文不可见。

## 验收标准

- **可复现实物**：防火墙规则脚本 + VPN 配置文件 + docker-compose。
- **验证证据**：nmap 扫描结果 + VPN 隧道内外抓包对比。
- **理解性说明**：iptables 规则匹配逻辑；VPN 隧道的封装与加密层次。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "net-sec-adv": `# 网络安全 · [挑战] 容器化拓扑注入网络条件：评估协议鲁棒性

## 实验目标

在容器化网络拓扑中注入劣化条件（延迟/丢包/重放），评估协议的鲁棒性。

## 实验要求

1. 使用 Docker + tc（traffic control）构建 client-server-attacker 三节点拓扑。
2. 注入网络条件：延迟（100ms+）、丢包（10%+）、包重放。
3. 在劣化条件下运行目标协议（如 TLS 握手、VPN 隧道、自定义认证协议）。
4. 记录协议行为：是否超时？是否重传成功？是否被重放攻击？

## 验收标准

- **可复现实物**：docker-compose + tc 配置脚本 + 测试脚本。
- **验证证据**：各条件下的协议行为记录（成功/超时/失败）+ 抓包证据。
- **理解性说明**：协议设计中的超时/重传/防重放机制分析；鲁棒性结论。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tpm-basic": `# 可信计算 · [基础] PCR 度量/对象概念/工具链入门

## 实验目标

理解 TPM 的基本概念（PCR、密钥层次、NV 索引），掌握 tpm2-tools 的基本操作。

## 实验要求

1. 搭建 TPM 模拟环境（swtpm + tpm2-abrmd）或使用真实 TPM。
2. 使用 tpm2-tools 完成基本操作：读取 PCR 值、扩展 PCR、创建主密钥。
3. 解释 PCR 度量链的含义：PCR[0]~PCR[7] 分别记录什么？
4. 创建 NV 索引并读写数据，理解 NV 空间的用途。

## 验收标准

- **可复现实物**：环境搭建脚本 + 操作命令记录。
- **验证证据**：PCR 读取输出 + 密钥创建输出 + NV 读写输出。
- **理解性说明**：TPM 密钥层次结构；PCR 度量链的信任传递。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tpm-inter": `# 可信计算 · [进阶] 密封/解封/策略绑定（状态绑定密钥）

## 实验目标

理解 TPM 密封（Seal）与解封（Unseal）机制，实践将数据绑定到平台状态。

## 实验要求

1. 使用 tpm2_create 创建密封对象，将一段数据密封到当前 PCR 值。
2. 正常解封：PCR 值未变时成功解封数据。
3. 修改 PCR 值（tpm2_pcrextend），再次尝试解封——应失败。
4. 解释策略绑定的含义：密钥只在"平台处于预期状态"时可用。

## 验收标准

- **可复现实物**：密封/解封脚本 + PCR 扩展脚本。
- **验证证据**：解封成功输出 + PCR 变更后解封失败输出。
- **理解性说明**：Seal 的 policy 绑定机制；PCR 值变更如何使密钥不可用。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tpm-adv": `# 可信计算 · [挑战] 最小远程证明闭环：Quote/Verify 语义分析

## 实验目标

完成最小远程证明（Remote Attestation）闭环，并明确说明"证明了什么、没证明什么"。

## 实验要求

1. 使用 tpm2_quote 生成 PCR 引用（Quote），用 AK（Attestation Key）签名。
2. 在验证方使用 tpm2_checkquote 验证签名与 PCR 值。
3. 分析：Quote 证明了什么？（平台状态符合预期）。没证明什么？（不证明软件无漏洞）。
4. 讨论：如果攻击者可以修改 BIOS 固件，Quote 还可信吗？

## 验收标准

- **可复现实物**：Quote 生成 + 验证脚本。
- **验证证据**：Quote 生成输出 + 验证通过输出 + 篡改后验证失败输出。
- **理解性说明**：远程证明的信任假设与边界；Quote 的语义精确描述。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tee-basic": `# 机密计算 · [基础] 环境部署与基本示例跑通

## 实验目标

搭建 OP-TEE on QEMU 开发环境，编译运行基本示例，理解 TEE/REE 分离架构。

## 实验要求

1. 按照 OP-TEE 官方文档，编译 QEMU + OP-TEE（使用 repo init + make）。
2. 启动 QEMU，确认 Normal World（Linux）和 Secure World（OP-TEE OS）均正常。
3. 运行 xtest（OP-TEE 自带测试套件），确认基本功能正常。
4. 运行 hello_world TA 示例，观察 CA（Client App）调用 TA 的输出。

## 验收标准

- **可复现实物**：编译/运行脚本或 Makefile。
- **验证证据**：xtest 通过输出 + hello_world 运行输出。
- **理解性说明**：TEE/REE 的角色划分；CA-TA 调用的基本流程。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tee-inter": `# 机密计算 · [进阶] CA-TA 调用链路 + 共享内存与边界分析

## 实验目标

深入理解 CA-TA 参数传递机制（共享内存），分析 TEE/REE 边界。

## 实验要求

1. 编写一个自定义 TA：接收 CA 传入的数据，在安全世界内处理后返回结果。
2. 使用 TEEC_SharedMemory 传递大块数据（如一个文件内容），观察性能。
3. 分析边界：哪些数据在 REE 侧可见？哪些只在 TEE 内存中？
4. 画出数据流图：CA → TEE Driver → OP-TEE OS → TA，标注每段数据的可见性。

## 验收标准

- **可复现实物**：自定义 TA 源码 + CA 源码 + Makefile。
- **验证证据**：CA 调用 TA 的输出 + 共享内存传递成功的证据。
- **理解性说明**：数据流图 + TEE/REE 边界处的安全假设。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,

  "tee-adv": `# 机密计算 · [挑战] 关键步骤入 TA：敏感数据生命周期与最小暴露证据

## 实验目标

将密码/协议的关键步骤移入 TA 执行，给出敏感数据生命周期与最小暴露的证据。

## 实验要求

1. 选择一个密码运算（如 AES 加密、RSA 签名），将密钥生成和运算放入 TA。
2. CA 侧只传入明文/待签数据，密钥永远不出 TEE。
3. 分析敏感数据（密钥）的生命周期：在哪生成？存在哪？何时销毁？REE 能否访问？
4. 给出"最小暴露"证据：通过日志/内存 dump 证明密钥未出现在 REE 地址空间。

## 验收标准

- **可复现实物**：TA 源码（含密钥管理）+ CA 源码 + Makefile。
- **验证证据**：运算正确输出 + REE 侧内存/日志中无密钥出现的证据。
- **理解性说明**：敏感数据生命周期图；最小暴露原则的设计决策。

## 提示

- 在 /lesson 的操作列表中选择「生成报告」，生成包含过程证据和服务器签名的实验报告。
`,
}
